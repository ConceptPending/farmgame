node_modules/
dist/
.next/
.turbo/
.vercel/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Local-only helper scripts (Puppeteer captures, balance probes) — the
# underscore prefix marks them as never-committed; see CLAUDE.md.
tools/_*
